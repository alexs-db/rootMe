This challenge is maybe difficult but I found a nice online tool which help you a lot.

So you already know how to convert binary you will immediately see something. All the 0 and 1 are regrouped by 8 bits and separated with space.

I resolve this challenge by logic :

- Convert all 8 bits binary numbers into decimal.

- Use a free online tools to convert decimal circular shift with the result.

- After make this circular bit shift, convert the decimal result into ASCII text.

GL !