For this challenge you have to think about "double extensions".

In the document on the subject you can see that there is flaws present on the server and the possibility of uploading PHP provided the extension is doubled with an image format.

So you have to find the '.passwd' file. 

For that, you must try to upload a file with the '.php.jpg' extensions.

In your file you can try some commands in PHP which the server will interpret as PHP.

Search about 'shell_exec' in PHP for example ;)

GL ! 